## Verification

All release assets are listed in `SHA256SUMS`:

```sh
sha256sum -c SHA256SUMS --ignore-missing
```

Each asset carries SLSA build provenance generated by GitHub Actions:

```sh
gh attestation verify <asset> -R __REPO__
```
